version: '3.8'

services:
  bot:
    build: .
    container_name: lor-helper-bot
    restart: always
    environment:
      - BOT_TOKEN=${BOT_TOKEN}
    volumes:
      - ./data:/app/data
      - ./logs:/app/logs
    command: python bot.py
